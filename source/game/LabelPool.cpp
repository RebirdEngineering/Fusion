#include <game/common/LabelPool.h>

//Classic? (TW for asserts?)

namespace game
{

/*LabelPool::LabelPool()
{
}

LabelPool::~LabelPool()
{
}

void LabelPool::addLabel(const std::string& name, int size, int color, const std::string& str, int style, Label* label)
{
	
};*/

}

//assert(m_users == 0)
//assert(it != m_labels.end())